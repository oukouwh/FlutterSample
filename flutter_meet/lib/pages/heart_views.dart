import 'package:flutter/material.dart';

class HeartViews extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('HEART')),
      body: Center(child: Text('HEART'),),
    );
  }
}