import 'package:flutter/material.dart';

class UsertViews extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('USER')),
      body: Center(child: Text('USER'),),
    );
  }
}